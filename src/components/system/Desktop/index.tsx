import React from 'react'
import { tw } from 'twind'

const Desktop: React.FC = () => {
    return (
        <div className={tw`w-0 h-[calc(100vh-45px)] flex flex(col wrap) gap-6`}>
            {/* TODO: Applications list */}
        </div>
    )
}

export default Desktop
